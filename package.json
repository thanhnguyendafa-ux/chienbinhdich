{
  "name": "chien-binh-dich",
  "version": "1.15.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test tests/*.test.js",
    "lint:content": "node src/data/validateContent.js",
    "check:syntax": "node scripts/checkSyntax.mjs",
    "check:architecture": "node scripts/checkArchitecture.mjs",
    "ci": "npm run check:syntax && npm run check:architecture && npm run lint:content && npm test"
  }
}
