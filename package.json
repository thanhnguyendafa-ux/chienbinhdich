{
  "name": "chien-binh-dich",
  "version": "1.15.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test tests/*.test.js",
    "lint:content": "node src/data/validateContent.js",
    "check:syntax": "node scripts/checkSyntax.mjs",
    "prepare:media": "node scripts/prepareMediaBundles.mjs",
    "ci": "npm run prepare:media && npm run check:syntax && npm run lint:content && npm test"
  }
}
